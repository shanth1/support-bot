templates:
  admin_notification: |
    🚀 <b>{{.ProjectName}}</b> [{{.Type}}]
    <b>From:</b> {{.Name}}
    <b>Message:</b> {{.Message}}
  user_forward: |
    👤 <b>New Message</b>
    <b>Project:</b> {{.ProjectName}}
    <b>User:</b> {{.UserName}} (@{{.UserHandle}})

messages:
  user_sent_ok: "✅ Message delivered to support. Please wait for a reply!"
  admin_sent_ok: "✅ Reply sent to user."
  error_user_blocked: "❌ Failed: user blocked the bot."
  error_not_found: "❌ Error: user not found in database."

projects:
  - id: "default"
    name: "General Support"
    topic_id: 0
    greeting: "Hello! Type your question below."
